/**
 * GRPC: Remote Procedure Call (RPC)
 * gRPC: works on RPC
 * RPC 
 * ProtoBuf
 * HandsOn 
 * REST Vs gRPC 
 * Pros/Cons
 * 
 */

/**
 * gRPC --> client can call a function which is located in a server
 * effective communication b/w two systems, it helps in load balancing,
 * auth, consider it's a framework which is built on top of RPC.
 * 
 * RPC: remote machines
 */